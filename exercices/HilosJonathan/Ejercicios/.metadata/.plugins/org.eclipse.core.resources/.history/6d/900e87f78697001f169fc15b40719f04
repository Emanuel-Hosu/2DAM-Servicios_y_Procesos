package main;

public class SubThread extends Thread {
    int diff = 0;
    public void run () {
        for (int i = 50; i >= 50; i--) {
            diff -= i;
            System.out.println ("the substraction is :" + diff);
        }
    }
}
